# Last-VLA Hard-Bottleneck Legacy Configs

These configs are deprecated hard-bottleneck / summary replacement variants.

Do not use them for formal training. The formal SOTA-oriented path is:

`configs/last_vla_v2/decoupled_highcap_no_risk/`

Formal Last-VLA v2 preserves raw VLM tokens and injects latent CoT through the zero-init decoupled residual branch.
